# Integration tests

These tests exercise the FastAPI contract across health, projects, prediction, SHAP explainability, What-If simulation, and recommendations.

Run locally with:

```bash
pytest -v
```
